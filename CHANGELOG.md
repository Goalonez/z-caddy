# Changelog

All notable changes to this repository will be documented in this file.

This project follows the upstream Caddy version for published release tags. Repository releases are published as `v<upstream-caddy-version>`.

## Unreleased

- Add multi-stage Docker build for custom Caddy images using `xcaddy`
- Add `modules.txt` to manage bundled Caddy modules in one place
- Add local build scripts for exporting offline Docker image archives and SHA256 files
- Add GitHub Actions for smoke builds and manual releases
- Add Docker Hub publishing workflow support
- Add user-facing README and repository ignore files
